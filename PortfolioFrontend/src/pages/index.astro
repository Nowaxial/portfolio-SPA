---
import MainLayout from '../layouts/MainLayout.astro';
import { PortfolioApp } from '../components/PortfolioApp';

// Pass empty arrays - PortfolioApp will fetch data client-side
// This allows the build to complete without requiring the API to be running
---

<MainLayout title="Portfolio | System Developer">
	<PortfolioApp projects={[]} skills={[]} recommendations={[]} client:load />
</MainLayout>
